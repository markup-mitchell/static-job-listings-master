{
  "name": "static-job-listings-master",
  "version": "1.0.0",
  "description": "Job listings with filtering coding challenge",
  "Xbrowserslist": "> 0.5%, last 2 versions, not dead /* https://github.com/parcel-bundler/parcel/issues/7101 */",
  "scripts": {
    "start": "cp -R ./src/images ./dist && parcel ./index.html --no-hmr",
    "winstart": "cp -R ./src/images ./dist && parcel ./index.html --no-hmr",
    "build": "parcel build index.html"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.0.0"
  },
  "dependencies": {
    "@swc/helpers": "^0.2.13",
    "lit": "^2.0.2"
  }
}
